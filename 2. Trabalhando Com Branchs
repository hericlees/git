# Esse comando mostra para nós quais são todas as ramificações presentes no nosso projeto

git branch

# Cria uma nova branch e altera de branch
git checkout -b testes (testes= nome da branch)

# Alterar de branch

git checkout + (o nome da branch. ex.: main)
ex.: git checkout main

# Mesclando branchs ( primeiro alternar para a branch main e depois integrar as alterações da branch)

git merge testes

# Excluindo uma branch

git push origin testes --delete

# Adicionando tags

git tag -a v1.4 -m “Versão 1.4” (Nesse comando, com a tag -a você define a versão, e com a tag -m você especifica uma mensagem para essa tag.)

# Enviando as tags para o servidor remoto

git push origin v1.4

# Para listar todas as tags do seu repositório

git tag
